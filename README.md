```
projeto-root/
│
├── .env
├── .gitignore
├── package.json
│
├── src/
│   ├── app.js                    // Arquivo principal da aplicação
│   ├── config/
│   │   ├── passport.js            // Configurações do Passport.js
│   │   └── connectionDB.js        // Configurações de conexão com o MongoDB
│   ├── controllers/
│   │   └── userController.js      // Lógica de autenticação e CRUD dos usuários
│   ├── models/
│   │   └── user.js                // Modelo do usuário para o MongoDB
│   └── routes/
│       └── user.js                // Rotas relacionadas aos usuários


```
